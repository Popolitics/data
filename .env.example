# Mot de passe du Postgres interne de Kestra (stockage des flows/exécutions,
# PAS le datamart applicatif). Générer une valeur forte par environnement,
# ex. : openssl rand -base64 24
KESTRA_DB_PASSWORD=change-me
